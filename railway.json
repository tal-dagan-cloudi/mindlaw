{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd mindlaw-landing && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd mindlaw-landing && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
